---
"mattpocock-skills": minor
---

Add `supervise` for coordinating a specific task or issue through verified completion. It assigns bounded work, follows workers through their results, checks progress against the parent acceptance criteria, and reassesses repeated failures before another attempt.

Ask once for the worker model and reasoning effort, offering Sol / Medium by default. Preserve settings already specified for the assignment, carry the selected pair across tickets, and pass it explicitly to desktop task tools.

Include desktop coordination guidance, invocation metadata, human-facing docs, and router registration. Keep the parent desktop thread as supervisor and give every delegated ticket its own dedicated desktop thread. Subagents are not a substitute. Preserve existing work when handing it over from subagents, carry forward task-creation authority and requested model settings, and report missing desktop tooling instead of changing execution modes. Saved goals and scheduled monitoring follow explicit user requests.
